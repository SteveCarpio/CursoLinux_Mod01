#!/bin/bash
# Diseña un script que realice lo siguiente:
# 1- Utilza el comando sudo para instalar un programa de tu elección utilizando apt-get. 
#    Por ejemplo, puedes instalar "tree" con sudo apt-get install tree.
# 2- Luego, espera la entrada del usuario para continuar, indícale que ya se ha instalado y 
#    que como es una prueba debe pulsar enter para desinstalar.
# 3- Una vez que el usuario presiona Enter, utiliza sudo apt-get remove para desinstalar el 
#    programa.

echo "> sudo apt-get tree"

echo "> sudo apt-get remove tree"

